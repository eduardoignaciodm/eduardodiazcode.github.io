image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp profile.jpeg public/
    - cp -r css public/
    - cp -r js public/
  artifacts:
    paths:
      - public
  only:
    - main